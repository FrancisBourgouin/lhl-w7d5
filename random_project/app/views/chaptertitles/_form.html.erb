<%= form_for(@chaptertitle) do |f| %>
  <% if @chaptertitle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chaptertitle.errors.count, "error") %> prohibited this chaptertitle from being saved:</h2>

      <ul>
      <% @chaptertitle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
